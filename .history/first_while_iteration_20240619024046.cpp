#include "std_lib_facilities.h"

int main() {
    // necessary initialization before loop
    int arr[a,i] = {97,1};
    auto [a,i] = arr;

    // while statement

    // check
    cout << a << ", " << i << "\n";
}